pipeline {
        agent none
        stages {
         
          stage("build & SonarQube Scanner") {
            agent any
            steps {
              withSonarQubeEnv('AMS-pipeline') {
                sh 'mvn clean package sonar:sonar'
              }
            }
          }
        }
      }
